description = 'iam-sdk Spring Boot Starter'

// Publishing configuration overrides
ext {
    publishingConfig = [
        groupId: rootProject.group,
        artifactId: 'iam-sdk-spring-boot-starter',
        version: rootProject.version,
        name: 'Docusign Java SDK - Spring Boot Starter',
        description: 'Spring Boot starter for the Docusign Java SDK.',
    ]
}

dependencies {
    api project(':')
    api project(':iam-sdk-spring-boot-autoconfigure')
    api "org.springframework.boot:spring-boot-starter:${rootProject.springBootVersion}"
}

jar {
    dependsOn 'generatePomFileForMavenPublication'
    archiveBaseName = 'iam-sdk-spring-boot-starter'
    into("META-INF/maven/com.docusign/iam-sdk-spring-boot-starter") {
        from(layout.buildDirectory.file("pom.xml")) {
            // Only include if it exists to avoid build ordering issues
            duplicatesStrategy = DuplicatesStrategy.INCLUDE
        }
    }
}